const path = require("path");
const { sequelize } = require("../configs/database");

// Placeholder for future model initialization
// You can import and initialize models here later, e.g.:
// const User = require('./user')(sequelize);

module.exports = {
  sequelize,
};
